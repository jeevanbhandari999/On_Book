import 'package:flutter/material.dart';

class OrganizationDetailsPageUserSide extends StatelessWidget {
  const OrganizationDetailsPageUserSide({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('Welcome to organization details page')),
    );
  }
}
