import 'package:flutter/material.dart';

class OrganizationDetailsPageOwnerSide extends StatelessWidget {
  const OrganizationDetailsPageOwnerSide({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Welcome to the organization details page owner side'),
      ),
    );
  }
}
