import 'package:flutter/material.dart';

class AdminPostPage extends StatelessWidget {
  const AdminPostPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Admin is here')));
  }
}
